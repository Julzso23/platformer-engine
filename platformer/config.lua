local config = {}

config.title = 'Example game' -- The title of the game

config.tileSize = 64 -- The width and height of each tile

config.gravity = 1000 -- The gravitational acceleration (px/s^2) of any entities affected by gravity
config.terminalVelocity = 400 -- The maximum vertical velocity

return config
